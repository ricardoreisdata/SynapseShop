# PROMPTS.md — Histórico de Uso de IA (Synapse One)

Registro auditável e reproduzível de todo uso de IA generativa no projeto SynapseShop, conforme diretriz do curso (SpecDD + transparência).

## Como registrar

| Data | Contexto | Objetivo | Prompt usado | Modelo/Provedor | Resultado & Observações |
|---|---|---|---|---|---|
| — | Spec/Aula que motivou o uso | O que se queria alcançar | Texto literal do prompt | Modelo/Provedor | O que foi aceito, corrigido ou aprendido |

**Regras:**
- Nenhum uso de IA sem registro.
- Prompts transcritos literalmente, para reprodutibilidade.
- Resultado descreve o que foi aceito, corrigido ou descartado.

## Histórico

| Data | Contexto | Objetivo | Prompt usado | Modelo/Provedor | Resultado & Observações |
|---|---|---|---|---|---|
| 2026-09-14 | Aula 1 — Kick-off, formação e repositório | Entender a metodologia do curso e planejar a estrutura inicial do repositório (`README.md` e `PROMPTS.md`) | "Preciso aprender o que o curso tem a oferecer... Antes de fazer qualquer etapa do spec, faça uma pequena introdução, explicando de forma didática cada processo, o porquê, melhores práticas." | opencode/big-pickle | Definidos SpecDD, anatomia do README e template do PROMPTS.md. Criados `README.md` (visão do MVP de eletrônicos + arquitetura em 6 camadas), `PROMPTS.md` e `.gitignore`. Decisões: squad Synapse One, Ricardo como Tech Lead, domínio loja de eletrônicos. Repositório vinculado ao GitHub e marco zero publicado (`commit 1c74f27` → `push -u origin main`). |

| 2026-09-14 | Aula 1 — Git | Publicar o repositório no GitHub com segurança | "Este é o endereço do repositório: https://github.com/ricardoreisdata/SynapseShop.git" | opencode/big-pickle | Conectado `origin`, configurado `.gitignore` (exclui node_modules, segredos e artefatos), realizado commit do marco zero e push publicado em `main`. Sem segredos no repositório. |

| 2026-09-14 | Aula 2 — Esqueleto do projeto e conteinerização | Criar a estrutura em camadas (api/services/repositories) e conteinerizar um script dummy com Docker Compose | "Guie-me na criação do esqueleto em camadas, Dockerfile e docker-compose de um container Python simples (mensagem + loop idle)." | opencode/big-pickle | Estrutura de pastas criada com `.gitkeep`; criados `app.py` (print com `flush=True` para log imediato), `Dockerfile` (FROM python:3.12.10-slim, WORKDIR /app, COPY app.py ., CMD), `docker-compose.yml` (serviço api, build: .) e `.dockerignore`. Validado `docker compose up --build -d`: imagem construída, container Up e log "esqueleto ativo" exibido. Encerrado com `docker compose down`. |

| 2026-09-14 | Laboratório autônomo de Docker (pré-Aula 3) | Consolidar o ciclo de vida completo do Docker com um exemplo criado do zero | "Quero aprofundar Docker: criar um exemplo do zero e ver todas as etapas de vida (pull, build, run, logs, exec, inspect, stop/start, rm/rmi)." | opencode/big-pickle | Laboratório criado em pasta temporária (fora do repo): app HTTP stdlib + Dockerfile. Percorridos pull, build (com cache de camadas), run (-d, --name, -p 18080:8000, -e), acesso via host, logs, exec (isolamento: hostname=container ID, /app), inspect (PID no host vs PID 1; IP 172.17.0.2), stop (Exited 137 = SIGKILL), start (dados preservados), rm e rmi. Aprendizado consolidado antes da Aula 3. |

| 2026-09-18 | Formação Docker completa (pré-Aula 3) | Aprender Docker do início ao fim, didaticamente, executando eu mesmo todos os comandos | "Me ensine Docker do início ao fim: conceitos (container vs VM, imagem vs container, engine e registry), ciclo de vida completo, Dockerfile e boas práticas (non-root, slim, cache de camadas), multistage build, volumes, networking e docker-compose." | opencode/big-pickle | 8 módulos práticos executados pelo aluno na pasta de laboratório temporária: (1) conceitos; (2) ciclo de vida com hello-world e app própria; (3) Dockerfile e cache de camadas (build caiu de 10.7s para 2.5s); (4) multistage e `COPY --from=builder`; (5) volumes com contador de hits persistente (após `rm`+`run` o dado continuou em 4); (6) rede bridge e testes container→container (200 via IP interno); (7) docker compose orquestrando API+Postgres com DNS interno por nome de serviço (`db` → 172.18.0.2) e persistência após `down`+`up`; (8) HEALTHCHECK/`/health` em preparação. Correções aplicadas: `-f` significa *file* no build e *follow* nos logs; contexto de build ≠ conteúdo da imagem; porta do host é única (`port is already allocated`); permissões de usuário não-root em volumes (criar pasta com `chown` no Dockerfile); healthcheck precisa usar ferramentas presentes na imagem slim (sem curl → usar python+httpx). |

| 2026-09-18 | Aula 3 — Docker essencial | Evoluir o Dockerfile para multistage build com cache de dependências e usuário não-root, orquestrar API + Banco de Dados com Compose e expor rota `/health` | "Evolua o Dockerfile da Aula 2 para os requisitos da Aula 3: multistage build, cache eficiente de dependências, usuário não-root e HEALTHCHECK apontando para a rota /health. Depois crie o docker-compose.yml orquestrando a API e o PostgreSQL com variáveis essenciais, rede interna e volume de persistência." | opencode/big-pickle | Aplicados no repositório: `api/main.py` com rotas `/` e `/health` (+ self-check via httpx) e `api/requirements.txt` substituindo o `app.py` dummy da raiz; `Dockerfile` reescrito com builder (pip `--prefix=/install`), estágio final com usuário não-root, `HEALTHCHECK` usando python+httpx (imagem slim não traz curl) e `EXPOSE 8000`; `docker-compose.yml` com serviços `api` e `db` (postgres:17-alpine), rede interna `synapseshop-net` (banco não exposto ao host por segurança), volume `pgdata` e `depends_on`; `README.md` documentado com subir/validar/derrubar/logs. Escopo isolado (sem Django/FastAPI/mensageria/IA). Validado com `docker compose up -d --build`: API `Up (healthy)`, `/health` respondendo, `whoami` = `appuser`, PostgreSQL 17.11 acessível via `psql` e `docker compose down` preservando o volume `synapseshop_pgdata`. Entregue no branch `dev` (main mantido estável). |

| 2026-09-18 | Aula 4 — CRUD da API principal com DRF | Implementar a estrutura inicial da API com Django REST Framework sob `/api/v1/`, expondo `Category` e `Item` com CRUD completo, checklist IA-safe e coleção Postman | "Implemente a Aula 4 do spec (CRUD com DRF): modelo Category e Item com ModelSerializer e ModelViewSet registrados em DefaultRouter sob /api/v1/, Django configurado no container com PostgreSQL (env vars), entrypoint com migrate e healthcheck mantido. No final aplique o checklist IA-safe ao código e registre os prompts." | opencode/big-pickle | Aplicados no repositório: projeto Django em `api/` (projeto `config` + app `store`) substituindo o `main.py` stdlib da Aula 3; models `Category` (name unique, description, timestamps) e `Item` (name, `sku` unique, `price` DecimalField com `MinValueValidator`, stock, active, FK `PROTECT`); `CategorySerializer`/`ItemSerializer` com campos de leitura derivados (`item_count`, `category_name`) e `validate_price` → 400; `CategoryViewSet`/`ItemViewSet` (ModelViewSet, verbos padrão 200/201/204/400/404); `config/urls.py` com admins, `/health/` (JsonResponse, sem httpx) e include de `store.urls` (DefaultRouter em `categories`/`items`); `Dockerfile` agora faz `COPY api/ .`, `entrypoint.sh` (migrate --noinput + runserver) e HEALTHCHECK via urllib; `docker-compose.yml` com `db` healthcheck (`pg_isready`) e `depends_on: condition: service_healthy`, além de *bind mount* `./api:/app`; `.dockerignore`/`.gitignore` ganharam `postman/` e `*.sqlite3`. Criados `specs/IA_SAFE_CHECKLIST.md` (7 seções de checagem) e `postman/SynapseShop.postman_collection.json` (coleção v2.1 com CRUDs + `/health/`). **Intervenções manuais:** permissão explícita `AllowAny` (auth = fase Aula 7); remoção do `httpx` do serve (health com stdlib); bind mount viabiliza gerar migrations no host; validação de preço adicionada no Serializer; `.gitignore` tratando `*.sqlite3`. Decisão de workflow: **Conventional Commits** (padrão `feat(api): ...`) a partir desta entrega. Validado em runtime no container (`docker compose up -d --build`): migrations `0001_initial` geradas e aplicadas (PostgreSQL 17.11), `/health/` → 200, CRUD completo com `Invoke-WebRequest` (PowerShell; `curl.exe` falha na passagem de aspas pelo PS 5.1): POST categoria/item → 201, GET → 200, preço `0` → 400 (validação do Serializer), id `999` → 404, DELETE → 204. API `Up (healthy)` e db `Healthy` via `pg_isready`. |

| 2026-09-18 | Aula 5 — Microsserviço complementar com FastAPI | Criar o microsserviço de estoque (`inventory`) em FastAPI com modelos Pydantic, tipagem estática, respostas padrão, `/docs` e integração ao docker-compose, além de criar o `PROMPTS-TEMPLATE.md` | "Implemente a Aula 5 do spec: microsserviço inventory em FastAPI com modelos Pydantic (InventoryItem com quantity >= 0 e AdjustRequest com delta != 0), rotas mínimas de estoque (GET /health, GET /inventory, GET /inventory/{sku}, PUT /inventory/{sku}, POST /inventory/{sku}/adjust, DELETE /inventory/{sku}) com response_model e status codes padrão. Sem banco de dados e sem autenticação. Dockerfile próprio (multistage, usuário não-root, HEALTHCHECK via urllib) e integração no docker-compose.yml na porta 8001. Crie o PROMPTS-TEMPLATE.md padronizando os prompts da squad. Ao final aplique o checklist IA-safe e registre os prompts." | opencode/big-pickle | Aplicados no repositório: microsserviço em `inventory/` (`app/models.py` com Pydantic models `InventoryItem` e `AdjustRequest` com validações `Field(ge=0)`; `app/store.py` com repositório em memória tipado e seeds `NB-01`/`MOUSE-RGB-01`; `app/main.py` com FastAPI app usando `lifespan` no lugar do deprecated `on_event`, dependência via `Depends(get_store)` e rotas com `response_model`); `requirements.txt` (`fastapi>=0.115,<1.0`, `uvicorn[standard]>=0.30,<1.0`); `inventory/Dockerfile` multistage com contexto de build próprio (`./inventory`), usuário não-root, `EXPOSE 8001` e HEALTHCHECK via urllib; `docker-compose.yml` com serviço `inventory` na porta `8001:8001` na rede interna `synapseshop-net`; criado `PROMPTS-TEMPLATE.md` na raiz (estrutura de prompt: contexto/objetivo/restrições SpecDD/stack/formato de saída/critérios de aceite + exemplo preenchido); `README.md` atualizado com seção do inventory (rotas mínimas alinhadas, `/docs` e `openapi.json`) e `PROMPTS.md` registrado. **Intervenções manuais:** semântica REST do `PUT` corrigida para `200 OK` (upsert idempotente, não 201); uso de `lifespan` em vez de `on_event` (deprecation); validações de `delta != 0` e estoque não negativo no endpoint (400); healthcheck com urllib (imagem slim sem curl). Escopo isolado (sem banco/Alembic — Aula 6, sem JWT — Aula 7). Validação no container: pendente `/docs` e endpoints em runtime. |

| 2026-09-21 | Aula 6 — Modelagem Relacional, Índices e Migrações | Persistir o microsserviço `inventory` (FastAPI) no PostgreSQL com SQLAlchemy + Alembic, índices e regras de integridade, repositórios/serviço transacionais, testes transacionais com tempos, rollback seguro e registro de decisões técnicas | "COm base no spec specs_da_aula_6.md, planeje e implemente a Aula 6: modelagem relacional da entidade de estoque (inventory) com SQLAlchemy (sku unique + índice, quantity com CHECK >= 0, created_at/updated_at), migrações Alembic com versionamento e rollback seguro, repositório transacional (commit/rollback, upsert, adjust atômico com SELECT FOR UPDATE), serviço consumindo o repositório, testes transacionais iniciais com medição de tempos e registro de decisões técnicas no repositório. PROIBIDO antecipar JWT (Aula 7) e Redis (Aula 8). Ao final aplicar checklist IA-safe e registrar os prompts." | opencode/big-pickle | Aplicados no repositório: camada de persistência em `inventory/` — `app/db.py` (engine `postgresql+psycopg`, `SessionLocal`), `app/entity.py` (modelo ORM `InventoryRecord` com PK, UNIQUE+índice em `sku`, CHECK `quantity >= 0`, timestamps), `app/repository.py` (`InventoryRepository`: get/list/upsert/delete/adjust com `with_for_update` e rollback em estoque negativo), `app/service.py` (`InventoryService` consumindo o repositório) e `app/main.py` reescrito com sessão por request (dependency com `yield`); `alembic.ini` + `alembic/env.py` + migração inicial `2026092101` (cria `inventory` com PK, UNIQUE, CHECK e índice); `inventory/Dockerfile` agora faz COPY de `alembic/` e `scripts/` e o CMD executa `alembic upgrade head` antes do `uvicorn`; `docker-compose.yml` com env DB_* e `depends_on: service_healthy` no inventory; `scripts/run_transactional_tests.py` (9 testes transacionais + tempos, média ~18 ms); criado `DECISOES_TECNICAS.md` (ADR-001 e ADR-002); README.md documentado (modelagem, migrações/rollback, arquitetura interna, comandos). **Validações em runtime:** `upgrade head` aplicado no start; `downgrade base` removeu a tabela e `upgrade head` restaurou (rollback seguro); endpoints /health, /inventory, PUT, adjust (200/400/404) e /docs validados com persistência real. **Intervenções manuais:** entidade-base = inventory (User/Token/Pedido rejeitados por conflito com a anti-antecipação do SpecDD); `get_service()` com dependency `yield` para fechar a session; `store.py` removido (substituído por repository/service); fix de `sys.path` no script de testes; `scripts/` adicionado ao COPY do Dockerfile (rebuild necessário).
